import React from "react";
import styles from "./counter.module.scss";
type Props = {};

const Counter = (props: Props) => {
  return <div className={styles.kek}>Counter</div>;
};
export default Counter;
